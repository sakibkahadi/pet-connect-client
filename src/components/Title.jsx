

const Title = ({subHeading}) => {
    return (
        <div>
            <h4 className="text-[#e63777] font-italic uppercase text-l font-bold md:text-l lg:text-xl text-center mb-6" >{subHeading}</h4>
        </div>
    );
};

export default Title;