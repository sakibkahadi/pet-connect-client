
const MainTitle = ({heading}) => {
    return (
        <div>
            <h1 className="text-[#ffffff] text-3xl md:text-4xl lg:text-6xl text-center mb-6" >{heading}</h1>
        </div>
    );
};

export default MainTitle;